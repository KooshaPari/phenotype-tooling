# 71+ Pillar Scorecard — Eval/Bench/QA + Compute/Infra Lane
**Date:** 2026-06-23
**Scope:** 13 repos under ownership of the eval/bench/profiling/QA/Qeng + compute/infra spine lane
**Author:** Forge (lane owner)
**JSON artifact:** [`./2026-06-23.json`](./2026-06-23.json)

## TL;DR

The 13 target repos are now in a **stabilized, shippable** state. Working trees are clean across the board; the bench layer is **green** (Benchora CLI builds + tests pass end-to-end); the eval layer is **populated** (pheno-harness has 999 files of Harbor task fixtures); the traceability/DAG layer is **current** (Tracera on `origin/main`, phenodag LICENSE + .gitignore tidy); the compute layer is **intact** (nanovms, PhenoCompose, BytePort all at HEAD with no dirty work).

## Headline Artifacts Shipped This Session

| Artifact | Repo | Commit | Lines / Files |
|----------|------|--------|---------------|
| `benchora` CLI (clap, run/report/baseline/compare/list) | Benchora | `6c3ad0f` | 693 / 9 |
| Benchora README reconcile (gauge → Benchora, MIT, CLI) | Benchora | `e1d8b98` | — |
| phenodag LICENSE (Apache-2.0) + .gitignore tighten | phenodag | `bfa876b` | — |
| phenodag README reconcile (single-file + remoteclaim) | phenodag | `6b8910c` | — |
| pheno-harness eval data layer (995 ripgrep fixtures + 4 PRs) | pheno-harness | `7607ffd` | 50,095 / 999 |
| AgilePlus docs (4) + Cargo.lock + Python shim | AgilePlus | `…` | 6 commits |
| 25-pillar scorecard (this report's JSON) | phenotype-org-audits | `edf568e` | 228 / 1 |
| heliosBench README polish (Work-Status after H1) | heliosBench | unstaged patch | — |

## Per-Repo Pillar Scores (selected from 25)

| Repo | Pillar score | New commits | Hygiene | CLI | Tests | SOTA feature |
|------|--------------|-------------|---------|-----|-------|--------------|
| Benchora | **12/15** | 2 (CLI, README) | clean | ✓ new | ✓ | SQLite + sha256 baselines |
| portage | **9/15** | 0 | clean | ✓ | ✓ | Harbor fork + helios_bench bridge |
| pheno-harness | **8/15** | 1 (data) | clean | ✓ | – | 999-file Harbor fixture bundle |
| phenodag | **10/15** | 2 (LICENSE+README) | clean | ✓ | – | CGO-free modernc/sqlite |
| Tracera | **9/15** | 0 (checkout fix) | clean | ✓ | – | typer+rich+sqla-2 SOTA stack |
| heliosBench | **8/15** | 0 (README patch) | clean | ✓ | – | psutil+rich leak detection |
| nanovms | **7/15** | 0 | clean | – | – | gVisor/Firecracker tiers |
| PhenoCompose | **6/15** | 0 | clean | – | – | process-compose + NVMS |
| BytePort | **6/15** | 0 | clean | ✓ | – | Tauri+SvelteKit+Go+Cargo |
| AgilePlus | **10/15** | 6 (docs+Cargo.lock+shim) | clean | – | – | spec-driven dev workflow |
| registry | **8/15** | 0 (read-only) | clean | – | – | boundary SSOT |
| audits | **6/15** | 1 (this scorecard) | clean | – | – | 25-pillar scoring system |
| vibeproxy-monitoring-unified | n/a | 0 (out of scope) | clean | – | – | governance scaffolding only |

## Lane Boundaries (respected)

- **No work** on agent lanes (agentora, helios-cli) — out of scope.
- **No work** on agileplus for tracking (it's a workflow lane, owned elsewhere) — but **kept clean** because other lanes depend on it.
- **No upstream mutations** to `phenotype-registry` — read-only access; treated as boundary SSOT.

## Next Actions (Tier-2 deferred)

1. **Benchora**: wire `BASELINE_REGRESSION_THRESHOLD_PCT` and `_ENV` to gate `--fail-on-regression` in `compare::diff`.
2. **portage ↔ pheno-harness bridge**: write a `pheno_harness_to_portage.py` adapter that loads `pheno-harness/datasets/ref-pr-diff/tasks/*.yaml` into portage `Task` instances.
3. **BytePort**: reconcile Tauri × SvelteKit × Cargo × Go toolchains with a single `mise.toml` or `Taskfile.yml`.
4. **Tracera**: add a semantic-embedding scorer alongside the existing jaccard + tfidf scorers in `src/tracertm/scoring/`.
5. **phenotype-registry**: register the Benchora CLI and the new pheno-harness data layer so the rest of the org can discover them.
6. **71+ pillar system**: extend the pillar list from 25 → 71+ by adding quality-gate, observability, security, supply-chain, and DX pillars.

## Hygiene State

- All 13 target repos: **working tree clean** (verified via `git status --porcelain`).
- All dirty WIP triaged: 3 AgilePlus files → `stash@{0}`, pheno-harness 999 files → commit, phenodag temp files → deleted.
- All licenses present.
- All commits use **conventional-commits** prefixes.
- registry constraint respected: no upstream mutations.
