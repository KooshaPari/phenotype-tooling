# Known Issues

## Current

- Canonical `phenotype-ops-mcp` has unrelated local changes and untracked docs.
- The prepared badge branch is based on local `main`, not the dirty active
  `fix/deps-2026-04-27` checkout.

## Next Action

Merge only after deciding whether the active dependency branch or `main` is the
correct integration base.
