# Research

## Repository Fit

`phenotype-ops-mcp` is an MCP server for ops/nanoVM management. The README
documents Claude Desktop MCP configuration, which makes it an agent-facing MCP
surface.

## Local State

The canonical checkout has unrelated local changes across Go module files,
configuration docs, worklogs, and AGENTS/docs additions. The rollout used an
isolated worktree from `main`.

## Governance Context

projects-landing tracks sladge as governance metadata. This rollout should not
change runtime code or catalog metadata.
