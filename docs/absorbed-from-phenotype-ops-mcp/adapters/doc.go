// Package adapters contains hexagonal adapter implementations for ops-mcp.
//
// These adapters depend only on the local core contracts so they can be wired
// into different entrypoints without coupling to the current main package.
package adapters
