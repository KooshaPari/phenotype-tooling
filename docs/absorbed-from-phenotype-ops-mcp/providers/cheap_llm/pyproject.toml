[project]
name = "cheap-llm-mcp"
version = "0.4.0"
description = "MCP server exposing Minimax + Kimi + Fireworks as a cheap Haiku-replacement reasoner"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0",
    "httpx>=0.27",
    "pydantic>=2.9",
    "tomli>=2.0; python_version<'3.11'",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24", "respx>=0.21"]

[project.scripts]
cheap-llm-mcp = "cheap_llm_mcp.server:main"
cheap-llm = "cheap_llm_mcp.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cheap_llm_mcp"]

[tool.pytest.ini_options]
markers = [
    "requirement: FR traceability marker for functional requirements",
]
