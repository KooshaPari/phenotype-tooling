# Research

## Repository Fit

PhenoMCP is the planned home for a Model Context Protocol server, backend
adapter crates, and cross-language bindings used by the Phenotype ecosystem.
Its local AGENTS contract describes it as a multi-language MCP SDK and runtime.

## Local State

The canonical checkout has unrelated local README/docs changes and untracked
governance files. The rollout used `PhenoMCP-wtrees/sladge-badge` from `main`.

## Governance Context

projects-landing tracks sladge as governance metadata only. This rollout should
not change runtime code or catalog metadata.
