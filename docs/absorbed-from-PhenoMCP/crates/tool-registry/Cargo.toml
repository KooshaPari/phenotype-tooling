[package]
name = "pheno-mcp-tool-registry"
version = "0.1.0"
edition = "2021"
description = "Extensible tool registry for PhenoMCP"
license = "MIT OR Apache-2.0"

[dependencies]
pheno-mcp-defs = { path = "../pheno-mcp-defs" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[dev-dependencies]
tempfile = "3"

[workspace]
