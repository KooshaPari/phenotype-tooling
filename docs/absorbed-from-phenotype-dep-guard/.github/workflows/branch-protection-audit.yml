name: Branch Protection Audit

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: "0 7 * * 1"

permissions:
  administration: read
  contents: read

jobs:
  branch-protection-audit:
    uses: KooshaPari/template-commons/.github/workflows/reusable-branch-protection-audit.yml@main
