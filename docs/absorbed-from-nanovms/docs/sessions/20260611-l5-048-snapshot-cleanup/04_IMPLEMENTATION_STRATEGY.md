# Implementation Strategy

- Use evidence from git history and filename pairs to limit cleanup scope.
- Prefer file deletions only; avoid changing code or workflow behavior.
- Preserve canonical worklog outputs already added on `main`.
