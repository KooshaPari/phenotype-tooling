# Coverage configuration for cargo-llvm-cov.
# ADR-023 Rule 3.1: lib tier >= 80% line coverage.

[report]
exclude = [
    "*/tests/*",
    "*/examples/*",
]
sort = "uncovered"

[html]
output = "target/llvm-cov-html"
