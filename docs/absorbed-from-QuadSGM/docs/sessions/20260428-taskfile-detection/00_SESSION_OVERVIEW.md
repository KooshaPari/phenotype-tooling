# Session Overview

- Goal: add a language-detecting `Taskfile.yml` with `build`, `test`, `lint`, and `clean`.
- Scope: preserve the repo's existing multi-language layout while replacing hardcoded task paths.
- Outcome target: a Taskfile that discovers manifests and runs the appropriate common commands.
