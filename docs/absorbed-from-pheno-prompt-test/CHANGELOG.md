# Changelog

## [Unreleased]

### Added
- pytest plugin: auto-collects `*.prompt` YAML files, runs them as regression tests.
- `PromptCase` dataclass + `prompt_case` factory.
- `pytest11` entry point (no conftest.py needed).
- YAML schema for prompt files.

## [0.1.0] - 2026-06-11

### Added
- First release.
