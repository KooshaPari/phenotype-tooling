# Research

- `pyproject.toml` defines a Python package named `omlx` with `uv`, `pytest`, and `ruff` in dev tooling.
- The repository root already had a Taskfile, but it was Python-only.
- The checkout also contains a clear Python source tree under `omlx/` and tests under `tests/`, so Python is the active language path in this repo.

