# Session Overview

- Goal: add a root `Taskfile.yml` with `build`, `test`, `lint`, and `clean` tasks.
- Scope: detect the repository language from project files and dispatch to the matching toolchain.
- Validation: parse the Taskfile with `task --list`.

