# Changelog

All notable changes to this project are documented here.

> **Note:** PhenoDevOps is a monorepo containing multiple subprojects (`agileplus`, `agileplus-agents`, `agileplus-mcp`, `dotfiles`, `clikit`, etc.). Each subproject manages its own version and release notes. Root-level release tags (CalVer) apply to the monorepo umbrella release.

Format: [CalVer](https://calver.org) — `YEAR.MONTH(WAVE).PATCH`

## [Unreleased]

### Added

- Monorepo umbrella structure with subprojects under `agileplus/`, `dotfiles/`, `clikit/`, `chaos/`, `capital/`, `ci/`
