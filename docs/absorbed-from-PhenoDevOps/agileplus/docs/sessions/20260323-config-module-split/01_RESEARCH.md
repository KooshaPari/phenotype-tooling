# Research

Local codebase research:
- Inspected `crates/agileplus-domain/src/config.rs` and usages via `rg`.
- Verified only `crate::config::*` is referenced outside the module.

External research: none.
