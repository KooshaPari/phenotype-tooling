coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 80%

comment:
  layout: "reach, diff, flags, files"
  behavior: default

ignore:
  - docs/
  - "**/tests/**"
  - "**/target/**"
  - "**/node_modules/**"
