# Validation

## Checks

- Search README and session docs for the Sladge badge label and URL.
- Review `git diff --stat` to confirm docs-only scope.
- Confirm the isolated worktree is clean after commit.

## Runtime Tests

No runtime tests are required because this change only updates README and session documentation.

