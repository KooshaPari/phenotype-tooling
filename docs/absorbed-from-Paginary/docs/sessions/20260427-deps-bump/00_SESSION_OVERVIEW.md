# Session Overview

- Goal: bump root workspace dependency resolution for `postcss`, `vite`, and `esbuild`.
- Success criteria: manifest override is committed, install completes, and the worktree is clean except for the intended change set.
- Branch: `fix/deps-2026-04-27`
