# Research

## Summary

This session tracks the policy wrapper standardization work for `policy-contract`.

## Relevant Context

- Host-side policy export needs to stay aligned with wrapper-runtime enforcement.
- Conditional host export behavior must remain additive and deterministic.
- Session artifacts should keep audit notes close to the policy implementation work.
- The repo is Python-first (`pyproject.toml`) with docs tooling in `package.json`, so the Taskfile should prefer Python commands while still detecting other supported manifests.
