name: ci
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: foo/bar@v4
      - uses: actions/checkout@v4
      - run: echo "fixture violation: foo/bar and actions/checkout unpinned above"
