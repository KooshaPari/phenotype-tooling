name: Alert sync issues
on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  sync:
    uses: KooshaPari/phenoShared/.github/workflows/alert-sync-issues.yml@bf2c65ac776d0c759406dfdb8551e6b344914ea0
    with:
      auto-label: auto-alert-sync
      min_severity: medium
