name: workflow-permissions
on: [push, pull_request]
permissions:
  contents: read
  pull-requests: write
  statuses: write
  security-events: write

jobs:
  permissions-audit:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow permissions baseline is configured."
