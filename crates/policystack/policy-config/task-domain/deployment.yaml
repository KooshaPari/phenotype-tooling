policy_version: v1
scope: task_domain
required_checks:
  - tests_integration
commands:
  deny:
    - "rm -rf .venv"
  require:
    - "python cli.py test run --scope integration"
security:
  pii_policy: strict
  guardrails:
    - no_undeclared_secrets
