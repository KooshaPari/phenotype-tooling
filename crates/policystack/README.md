# policystack

Content absorbed from KooshaPari/PolicyStack.

> **Note**: Full subtree merge is blocked on Windows due to CRLF-contaminated git tree
> entries (all filenames end in `\r`) in the source repo. This is an upstream repo-hygiene
> bug. Fix: run `git filter-repo --path-rename` to strip CRs from tree on Linux CI.
> Tracked: open upstream issue against KooshaPari/PolicyStack.

Source: https://github.com/KooshaPari/PolicyStack (115 commits, squash-pending)
