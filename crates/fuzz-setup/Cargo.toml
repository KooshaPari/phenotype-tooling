[package]
name = "fuzz-setup"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
serde_json = "1.0"
sha2 = "0.10"
arbitrary = { version = "1.3", features = ["derive"] }
libfuzzer-sys = "0.4"

