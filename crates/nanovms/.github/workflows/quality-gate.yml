name: Quality Gate

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Lint
        run: echo "Linting..."

      - name: Test
        run: echo "Testing..."

      - name: Coverage
        run: echo "Checking coverage..."
