[package]
name = "pheno-tracing"
version = "0.4.0"
edition = "2021"
description = "Stub crate re-exporting `tracing` to unblock offline builds (ADR-012 patch target)"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[lib]
path = "src/lib.rs"