# Known Issues

- The Python submodule is not initialized in this checkout.
- TypeScript is scaffold-only and has no `tsconfig.json` yet.
- `rust/Cargo.toml` already had a pre-existing modification in the worktree and was left untouched.
