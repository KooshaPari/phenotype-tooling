# Known Issues

- The repository was behind `origin/main` before branching.
- The Dependabot API on the remote default branch still reflects the pre-merge state until this branch is merged or pushed through CI.
