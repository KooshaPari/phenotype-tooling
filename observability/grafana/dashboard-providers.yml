apiVersion: 1

providers:
  - name: 'phenotype-default'
    folder: 'phenotype-tooling'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
