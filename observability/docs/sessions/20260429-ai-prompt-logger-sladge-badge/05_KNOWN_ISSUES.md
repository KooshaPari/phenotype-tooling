# Known Issues

## Open

- The prepared badge commit is not merged into the canonical checkout because
  the canonical `PhenoObservability` worktree has unrelated local changes.

## Resolved

- None. This pass is a governance metadata update only.
