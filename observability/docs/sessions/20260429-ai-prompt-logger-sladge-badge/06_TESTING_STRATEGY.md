# Testing Strategy

## Validation

- Confirm `sladge.net` appears in the README and session docs.
- Review `git diff --stat` for a documentation-only change set.
- Confirm worktree status is clean after commit.

## Not Run

- Runtime tests are not required because this change only updates documentation
  and governance metadata.
