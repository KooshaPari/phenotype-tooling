# Research

## Repository State

- Canonical PhenoObservability is clean on `main` at `892e20b`, locally ahead
  two and behind origin two.
- Older prepared branch `docs/phenoobservability-sladge-current` diverged by
  `3 7`, so commit `aa66c86` is stale evidence for the active local head.
- Repo-local instructions require worktrees under
  `repos/PhenoObservability-wtrees/<topic>/`.

## Existing Badge State

The current root README did not contain `sladge.net` or `AI Slop Inside` before
this refresh.
