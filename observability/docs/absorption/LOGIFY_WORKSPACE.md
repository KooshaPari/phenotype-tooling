# Logify → logkit workspace registration

**Date:** 2026-06-17  
**Wave:** 6

`crates/logkit` is registered in the PhenoObservability workspace so consumers (e.g. Pyron) can git-pin `logkit` from this repo instead of the legacy Logify repo.

Canonical observe-role owner: **PhenoObservability** per DOMAIN_ROLES.
