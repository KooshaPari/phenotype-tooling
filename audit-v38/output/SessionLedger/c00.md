CLUSTER_START cluster=C00 repo=SessionLedger pillars=L0-L9 date=2026-07-14
# Wave-33 reaudit tip 18486e8 (#273-#278)

### L0 — Architecture Foundations
score: 3  glyph: ✓
evidence:
  - src/lib.rs:3-19 — hexagonal layout documented: domain / ports / ingestion / distill / viewer / export
  - src/domain/mod.rs:1-9 — pure domain modules (acceptance, bundle, context, contract, dedup, intent, session); no I/O
  - src/ports/mod.rs:1-71 — port traits (CorpusSource, MemoryStore, Compressor, TraceSink, extractors) with thiserror PortError
  - src/distill/memory_writer.rs:17-70 — DistillMemoryWriter persists intent/contract/context facts behind MemoryStore and optional TraceSink ports
  - crates/sl-daemon/src/lib.rs:1-19 — daemon as adapter/ETL shell; tracing spans on worker/process_session
  - docs/DESIGN.md:16-46 — composition map and ContinuationBundle model as architectural SSOT
gaps:
  - none identified — effort: S
soft_goal_delta: +15% toward shared-kernel TraceSink/MemoryStore adapters (ports remain; OTel export still soft)

### L1 — Module Structure & Boundaries
score: 3  glyph: ✓
evidence:
  - src/lib.rs:21-36 — explicit pub mod + curated re-exports of domain/export/ingestion surface
  - src/distill/compiler.rs:1-31 — BundleCompiler orchestrates extractors behind port traits
  - crates/sl-daemon/Cargo.toml:36-42 — sl-daemon isolated workspace root so viewer webkit graph does not poison daemon builds
  - Cargo.toml:1-5 — workspace members=[sl-viewer], exclude=[sl-daemon] documents boundary
  - tests/skeleton.rs:1-30 — integration tests live at workspace root separate from unit modules
gaps:
  - none identified — effort: S
soft_goal_delta: +5% toward per-crate prelude.rs convenience re-exports

### L2 — API Surface & Contract
score: 3  glyph: ✓
evidence:
  - docs/api/openapi.yaml:1-368 — OpenAPI 3.1 contract for health, bundles, ingest, metrics, and SSE routes
  - crates/sl-daemon/src/http.rs:63,393-685 — Idempotency-Key header on POST /api/ingest with process-local replay cache and conflict envelope
  - crates/sl-daemon/src/http.rs:1174-1241 — ingest idempotency replay and conflict unit tests
  - crates/sl-daemon/src/http.rs:58-65 — public routes: /healthz, /readyz, /api/bundles, /api/search, /api/stream, /api/replay, /api/ingest, /api/metrics
  - crates/sl-daemon/src/validation.rs:1-58 — ingest contract with field/code/message ValidationError
  - docs/ops/local-trust-boundary.md — documents loopback-only binding and idempotency retention limits
  - crates/sl-daemon/src/http.rs:60-76,1112+ — OPENAPI_ROUTE_SURFACE SSOT for nine stable routes
  - crates/sl-daemon/src/http.rs:1174+ — unit tests compare SSOT to docs/api/openapi.yaml and axum registrations
  - scripts/openapi-drift-check.ps1 — PR/CI gate via cargo test openapi_route_surface
  - .github/workflows/ci.yml:26-37 — openapi-drift job on ubuntu-latest
gaps:
  - none identified for L2 contract drift — effort: S
soft_goal_delta: +95% toward schema drift CI (OpenAPI + idempotency + drift gate landed Wave-15 #148, Wave-16 #153)

### L3 — Data Model & State Management
score: 3  glyph: ✓
evidence:
  - src/domain/intent.rs:1-40 — structured Intent + lifecycle FSM aligned with forgecode DAG
  - src/domain/merge.rs:39-142 — deterministic duplicate-scoped merge with reversible DedupManifest
  - src/distill/memory_writer.rs:40-69 — session-scoped episodic intent/contract/context persistence through MemoryStore
  - docs/DESIGN.md:32-46 — Acceptance/Contract/Context/Intent bundle model
  - docs/functional_requirements.md:14-28 — FR-001..015 cover session/OKF/bundle public surface
  - src/schema/mod.rs:1-33 — ordered forward-only migration manifest (`initial_memory_facts` v1)
  - src/schema/migrate.rs:47-85 — `apply_all` with idempotent version tracking in `schema_migrations`
  - src/schema/migrations/001_initial.sql:1-19 — durable `memory_facts` table contract
  - docs/ops/schema-migrations.md:1-33 — operator SSOT for manifest, local check, and adapter limits
  - .github/workflows/ci.yml:49-57 — blocking `schema-migrate` job runs `cargo test --features sqlite schema::`
  - tests/schema_migrate.rs:1-13 — integration apply smoke for sqlite-gated migration path
  - src/ports/sqlite_memory.rs:16-181 — `SqliteMemoryStore` opens DB, calls `apply_all`, persists/recalls via `memory_facts`
  - src/ports/sqlite_memory.rs:134-181 — unit + integration tests (recall, migration-on-open, `DistillMemoryWriter` path)
  - src/ports/sqlite_memory.rs:53-63 — `ping()` readiness probe for optional memory DB dependency
  - crates/sl-daemon/src/main.rs:503-512,527-540 — optional `SL_MEMORY_DB` opens store and passes `MemoryStore` into ETL consumer
  - crates/sl-daemon/src/etl.rs:44-62,149-159 — `transform_file` persists distilled facts when memory store configured; unit test covers path
  - crates/sl-daemon/src/http.rs:209-211,278-305 — `/readyz` probes optional memory DB via `store.ping()`
  - .github/workflows/ci.yml:49-60 — `schema-migrate` job runs sqlite adapter tests plus sl-daemon sqlite wiring tests
  - docs/ops/schema-migrations.md:22-33 — operator SSOT documents adapter + local check commands
gaps:
  - CancellationToken end-to-end and additional durable store deps beyond sqlite memory — effort: M
soft_goal_delta: +95% — adapter scaffold + daemon runtime wiring + `/readyz` probe landed Wave-18 #165, Wave-19 #171, Wave-20 #173

### L4 — Async / Concurrency Design
score: 3  glyph: ✓
evidence:
  - crates/sl-daemon/src/main.rs:46-51 — CHANNEL_CAPACITY / BROADCAST_CAPACITY = 256 (bounded backpressure)
  - crates/sl-daemon/src/main.rs:334-336 — mpsc::channel(CHANNEL_CAPACITY) + broadcast for SSE fan-out
  - crates/sl-daemon/src/shutdown.rs:1-69 — `ServeShutdown` wraps `CancellationToken` for cooperative cancel (#190)
  - crates/sl-daemon/src/main.rs — serve path installs cancellation and drains watcher/HTTP on token cancel
  - crates/sl-daemon/src/watcher.rs — watcher loop observes cancellation token
  - crates/sl-daemon/src/http.rs:82 — axum::serve with with_graceful_shutdown
  - crates/sl-daemon/tests/sse_bridge.rs:116 — graceful shutdown exercised in tests
gaps:
  - Formal concurrency checkers (loom/shuttle) and cancel ADR for multi-host fans — effort: M
soft_goal_delta: +90% — CancellationToken end-to-end on serve landed Wave-22 #190

### L5 — Observability (logs / metrics / traces)
score: 3  glyph: ✓
evidence:
  - crates/sl-daemon/src/main.rs:320-385 — text/optional JSON tracing subscriber plus optional OTLP lifecycle
  - crates/sl-daemon/src/http.rs:64-76,142-149 — health/readiness, product metrics, and Prometheus /metrics routes
  - crates/sl-daemon/src/http.rs:190-229 — per-request RED timing and W3C traceparent fields
  - crates/sl-daemon/src/metrics.rs:9-50 — process-local request/error/duration Prometheus exposition
  - crates/sl-daemon/src/otel.rs:14-41 — feature-gated OTLP span exporter
  - docs/ops/observability.md:8-84 — operator contract for logs, health, metrics, RED signals, and traces
gaps:
  - Add OTLP metrics, exemplars, and dashboard provisioning — effort: M
soft_goal_delta: +90% toward full OTel stack (JSON logs + OTLP traces + Prometheus HTTP RED metrics landed)

### L6 — Performance Budgets & Profiling
score: 3  glyph: ✓
evidence:
  - Cargo.toml:38-43 — Criterion dev-dependency and pipeline bench target
  - benches/pipeline.rs:27-49 — distill, OKF export, and injection rendering benchmarks with 200-message throughput
  - docs/ops/perf-baseline.json — blocking mean budgets (`policy.enforced=true`) plus blocking p95 (`latency.enforced=true`) (#243/#256)
  - scripts/bench-gate.ps1 — `-SelfCheck` + `-SoftLatencyCheck`; p95 overruns fail when enforced (#256)
  - .github/workflows/bench-gate.yml — latency jobs blocking (no `continue-on-error`) beside mean gate (#256)
  - .github/workflows/ci.yml — pipeline latency companion jobs blocking (#256)
  - tests/perf_budget.rs — asserts enforced latency budgets and SoftLatencyCheck (#256)
  - docs/ops/perf-regression.md — documents enforced p95 vs mean policy (#256)
gaps:
  - Add flamegraph/stable-hardware profiling automation beyond Criterion gates — effort: M
soft_goal_delta: +100% — enforced p95 SoftLatencyCheck Wave-30 #256; flamegraphs remain (score held at pillar max)

### L7 — Concurrency Safety & Races
score: 2  glyph: ~
evidence:
  - Cargo.toml:46-47 — unsafe_code = forbid at workspace package
  - crates/sl-daemon/Cargo.toml:32-33 — unsafe_code = forbid on daemon
  - crates/sl-daemon/src/http.rs:82 — graceful shutdown reduces in-flight race on stop
  - crates/sl-daemon/src/main.rs:46-51 — bounded channels reduce unbounded-queue races
  - tests/race_model.rs — loom-lite bounded sync_channel + cooperative cancel with conservation/capacity asserts (#202)
  - tests/race_smoke.rs — merge/OKF race smoke retained alongside race_model
  - .github/workflows/race-smoke.yml — 3 OS × 3 repeats run race_smoke + race_model (#202)
  - docs/ops/concurrency-safety.md — L7 concurrency-safety operator contract (#202)
  - .github/workflows/miri-smoke.yml — soft nightly Miri on race_model (`continue-on-error`) (#235)
  - docs/ops/concurrency-safety.md — documents soft Miri path (#235)
  - tests/loom_model.rs — soft loom cancel/capacity model under `cfg(loom)` (#264)
  - .github/workflows/loom-smoke.yml — soft SelfCheck + loom_model (`continue-on-error`) (#264)
  - scripts/loom-smoke-check.ps1 — hermetic soft loom SelfCheck (#264)
  - MISSING: full loom / shuttle / blocking miri / TSan permutation CI jobs
gaps:
  - Promote loom/Miri off soft-only; add full shuttle permutation checkers for broadcast/SSE — effort: M
soft_goal_delta: +70% — soft loom smoke Wave-31 #264; full permutation checkers remain (score held)

### L8 — Memory & Allocation
score: 2  glyph: ~
evidence:
  - Cargo.toml:34 — tempfile dev-dep for isolated test dirs
  - crates/sl-daemon/src/metrics.rs:40-47 — TempDir-backed unit tests avoid shared state
  - tests/memory_budget.rs — ingest-path RSS budget smoke (#196)
  - scripts/rss-budget-check.ps1 — CI/ops RSS budget checker (#196)
  - docs/ops/memory-budget.json — budget SSOT for ingest RSS ceiling (#196)
  - docs/ops/memory-budget.md — operator contract for RSS smoke (#196)
  - .github/workflows/ops-load.yml — RSS budget smoke wired into load workflow (#196)
  - tests/allocation_budget.rs — counting-allocator Region deltas over process_session (#231)
  - scripts/allocation-budget-check.ps1 — self-check + soft ops-load companion (#231)
  - docs/ops/allocation-budget.json / docs/ops/allocation-budget.md — allocation ceiling SSOT (#231)
  - docs/ops/alloc-profile.md — optional `dhat` heap smoke contract (#250)
  - docs/ops/alloc-profile.json — dhat ceiling SSOT (#250)
  - scripts/alloc-profile-check.ps1 — SelfCheck + feature-gated dhat smoke (#250)
  - tests/alloc_profile_dhat.rs — dhat Profiler over process_session (#250)
  - tests/alloc_profile.rs — alloc-profile config SelfCheck (#250)
  - .github/workflows/ops-load.yml — soft `alloc-profile` job (`continue-on-error`) (#250)
  - crates/sl-daemon optional `jemalloc` feature + tikv-jemallocator Unix `#[global_allocator]` (#277)
  - docs/ops/jemalloc.md / scripts/jemalloc-check.ps1 / tests/jemalloc_soft.rs — soft SelfCheck (#277)
  - .github/workflows/ops-load.yml — soft `jemalloc` job (`continue-on-error`) (#277)
  - MISSING: default-on production jemalloc; Windows allocator parity; continuous dhat in production daemon
  - MISSING: bytes::Bytes / Cow adoption on hot I/O paths
gaps:
  - Promote jemalloc off soft/optional; continuous production allocator profiling — effort: M
soft_goal_delta: +80% — soft optional jemalloc feature Wave-33 #277; default-on / Windows unpaid (score held)

### L9 — Build & Release Pipeline
score: 3  glyph: ✓
evidence:
  - Cargo.lock:1 — committed lockfile (git ls-files); AGENTS.md:53 requires --locked
  - rust-toolchain.toml:1-5 — channel=stable + rustfmt/clippy components
  - .github/workflows/release.yml:28-49 — multi-OS matrix (linux/mac/win) for sl-viewer tags
  - .github/workflows/qgate.yml:57-73 — cargo-cyclonedx → target/sbom.cdx.json uploaded as sbom-cyclonedx
  - SECURITY.md:41-48 — documents CycloneDX SBOM + cargo-deny/gitleaks/cargo-audit
  - Containerfile:1-11 — multi-stage daemon image; crates/sl-daemon/Containerfile:41 USER sl
gaps:
  - Set SOURCE_DATE_EPOCH + attach SBOM on release tags (qgate uploads today) — effort: M
soft_goal_delta: +65% toward SLSA/repro (lockfile + SBOM artifact landed; provenance soft)

Wave-15: L2 committed OpenAPI + ingest Idempotency-Key (+1).
Wave-16: L2 OpenAPI drift CI gate (#153); score held at pillar max.
Wave-18: L3 durable schema migration scaffold + sqlite CI gate (#165); L3 +1.
Wave-19: L3 SqliteMemoryStore adapter + sqlite memory tests (#171); score held — L3 already pillar max.
Wave-20: L3 SqliteMemoryStore wired into daemon serve + `/readyz` memory_db probe (#173); score held — L3 already pillar max.
Wave-22: L4 cooperative CancellationToken on serve (#190); L4 +1.
Wave-23: L8 RSS memory budget smoke for ingest (#196); L8 +1.
Wave-24: L7 loom-lite race_model + race-smoke CI (#202); L7 +1.
Wave-27: L8 allocation-budget counting smoke (#231); score held — jemalloc/dhat unpaid.
Wave-29: L8 soft dhat alloc-profile smoke (#250); score held — jemalloc/production `#[global_allocator]` unpaid.
Wave-30: L6 enforce `latency.enforced=true` blocking SoftLatencyCheck (#256); score held — L6 already pillar max.
Wave-31: L7 soft loom concurrency smoke (#264); score held — soft `continue-on-error`; full loom/shuttle unpaid.
Wave-33: L8 soft optional jemalloc feature (#277); score held — soft `continue-on-error`; default-on/Windows unpaid.
CLUSTER_TOTAL score=28/30 pct=93% grade=A
CLUSTER_DONE cluster=C00 repo=SessionLedger
