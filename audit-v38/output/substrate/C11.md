CLUSTER_START cluster=C11 repo=substrate pillars=L108-L122 date=2026-07-04

# Profile: CLI + SDK library (Rust workspace, 54 crates). No GUI/desktop surface →
# installer/tray/GUI-splash pillars scored NOT_APPLICABLE in context; distribution
# surface = crates.io + release-binary + CLI channels.

### L108 — Native Installer(s)
score: 0  glyph: ✗
evidence:
  - NOT_APPLICABLE (partial): no GUI app; but no packaged CLI installer either (no .dmg/.deb/brew cask)
  - .github/workflows/release-binary.yml builds a binary artifact (raw binary, not an OS installer)
gaps:
  - No native installer / brew cask for the CLI — effort: M
soft_goal_delta: release-binary exists; no installer packaging

### L109 — CLI Packaging & Install Channels
score: 2  glyph: ~
evidence:
  - crates/driver-cli/Cargo.toml:10-11 [[bin]] name = "substrate" — installable via `cargo install`
  - README.md:161 crates publish-ready (`cargo publish --dry-run` green)
  - README.md:35 documents build; but no `cargo install`/brew one-liner in README
gaps:
  - No documented install one-liner (brew/cargo install) + not yet on crates.io — effort: S
soft_goal_delta: bin defined + publish-ready; channels/one-liner missing

### L110 — Tray / Menubar Client
score: 0  glyph: ✗
evidence:
  - NOT_APPLICABLE: substrate is a dispatch CLI/SDK; no daemon tray. (substrate-serve-lock is a lockfile, not a tray.) A TUI dashboard exists (task #69) but no OS tray client.
gaps:
  - none — not applicable (TUI dashboard covers at-a-glance need) — effort: S
soft_goal_delta: n/a

### L111 — Auto-Update
score: 1  glyph: △
evidence:
  - No self-update subcommand or updater found; cargo-install path implies `cargo install` re-run for updates
gaps:
  - No `self-update` subcommand or documented update channel — effort: S
soft_goal_delta: not started

### L112 — Code-Signing & Notarization
score: 0  glyph: ✗
evidence:
  - No codesign/notarytool/signtool step in .github/workflows/ (release-binary.yml ships unsigned binary)
gaps:
  - Unsigned release binary — Gatekeeper friction on macOS — effort: M
soft_goal_delta: not started

### L113 — Deploy Surface: Container / OCI
score: 0  glyph: ✗
evidence:
  - MISSING: Dockerfile / Containerfile / process-compose.yml (grep returned none)
  - driver-http crate provides an HTTP surface (README.md:169 SUBSTRATE_HTTP_BIND) but no container to run it
gaps:
  - No Containerfile for the driver-http surface — effort: M
soft_goal_delta: not started

### L114 — Deploy Surface: Serverless / Edge
score: 0  glyph: ✗
evidence:
  - NOT_APPLICABLE (partial): no vercel.ts/wrangler.toml; substrate is a local dispatch tool, edge deploy not a primary surface
gaps:
  - none if edge is out of scope; else wire driver-http to an edge target — effort: L
soft_goal_delta: n/a (out of scope)

### L115 — Deploy Surface: Traditional Server
score: 1  glyph: △
evidence:
  - driver-http crate (crates/driver-http/) exposes a REST surface bindable via SUBSTRATE_HTTP_BIND (README.md:169)
  - No Caddyfile/nginx/systemd unit committed to front it
gaps:
  - No reverse-proxy/systemd config for the HTTP surface — effort: M
soft_goal_delta: HTTP surface exists; no server deploy config

### L116 — Deploy-Surface Parity & Robustness
score: 1  glyph: △
evidence:
  - Primary surface = CLI (`cargo build --release -p driver-cli`) + SDK crate + driver-http; CLI path is proven (README.md:35, dogfooded per README.md:10)
  - No surface matrix documenting status/proof per target
gaps:
  - No deploy-surface matrix; container/server surfaces unproven — effort: M
soft_goal_delta: CLI surface solid; others unproven

### L117 — Mobile Presence Decision
score: 0  glyph: ✗
evidence:
  - No mobile decision documented (grep mobile/PWA/Expo in README/docs → only unrelated hits)
gaps:
  - Record a deliberate "no mobile — it's a dev CLI" rationale — effort: S
soft_goal_delta: not started (likely N/A but undecided)

### L118 — Release Automation
score: 2  glyph: ~
evidence:
  - .github/workflows/release-binary.yml + release-crates.yml — automated binary + crate release
  - README.md:161 crates publish-ready
gaps:
  - No changelog automation (CHANGELOG.md absent — see L119); asset signing missing — effort: S
soft_goal_delta: release pipeline present; changelog/signing gaps

### L119 — Versioning & Compatibility Policy
score: 1  glyph: △
evidence:
  - Cargo.toml:56 rust-version = "1.80" (MSRV declared — good)
  - MISSING: CHANGELOG.md (no changelog / breaking-change log)
gaps:
  - No CHANGELOG.md documenting versions/breaking changes — effort: S
soft_goal_delta: MSRV set; changelog missing

### L120 — Distribution Channels & Discoverability
score: 1  glyph: △
evidence:
  - Publish-ready crates (README.md:161) + release-binary workflow; llms.txt notes phenotype-registry membership
  - Not yet actually on crates.io; no README badges/homepage confirmed
gaps:
  - Publish to crates.io + add badges/homepage — effort: S
soft_goal_delta: registry-listed internally; public channels pending

### L121 — Install/Uninstall Cleanliness & QOL
score: 1  glyph: △
evidence:
  - `cargo install`/`cargo uninstall` provides clean bin lifecycle by default
  - Config/data path conventions not verified in-repo (no explicit dirs:: usage confirmed here)
gaps:
  - Document config/data locations + uninstall notes — effort: S
soft_goal_delta: cargo lifecycle clean; data-path docs missing

### L122 — Deploy Observability & Health Endpoints
score: 2  glyph: ~
evidence:
  - crates/driver-http/tests/api.rs — HTTP surface tested
  - substrate emits structured JSON dispatch results (per `substrate dispatch` contract); tracing likely via substrate-trace crate
gaps:
  - No confirmed /health endpoint on driver-http; otel export unverified — effort: S
soft_goal_delta: structured output + trace crate; health endpoint unconfirmed

CLUSTER_TOTAL score=13/45 pct=29% grade=F
CLUSTER_DONE cluster=C11 repo=substrate
