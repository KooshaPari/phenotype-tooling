CLUSTER_START cluster=C10 repo=substrate pillars=L96-L107 date=2026-07-04

# Profile: CLI + TUI (crates/substrate-tui: ratatui dashboard/statusbar/help). Scored
# in TUI context — web-only pillars (WCAG DOM, @font-face) map to TUI equivalents
# (color scheme, glyph styling, golden output).

### L96 — Design Token System
score: 1  glyph: △
evidence:
  - crates/substrate-tui/src uses styling (30 `Color::` refs across statusbar.rs/help.rs/dashboard.rs) but SCATTERED
  - MISSING: no central theme/palette/style module (ls src/theme.rs|palette.rs|style.rs → none)
gaps:
  - No token/palette SSOT; colors inline per-component — effort: M
soft_goal_delta: not started

### L97 — Typography System
score: 1  glyph: △
evidence:
  - TUI uses ratatui default text styling; deliberate layout (dashboard.rs, statusbar.rs) but no documented type/box-drawing style system
gaps:
  - No documented TUI typographic style (glyph set, alignment convention) — effort: S
soft_goal_delta: not started

### L98 — Icon Set
score: 0  glyph: ✗
evidence:
  - No .iconset/.icns; no tray icon (CLI/TUI, no OS surface). NOT_APPLICABLE for app icon; no unicode glyph system documented either.
gaps:
  - none (app-icon) not applicable; optional: define a glyph set doc — effort: S
soft_goal_delta: n/a

### L99 — Loading & Skeleton States
score: 2  glyph: ~
evidence:
  - crates/substrate-tui/src/components/dashboard.rs uses a progress/gauge indicator (grep indicatif/ProgressBar/Spinner/Gauge hit)
  - Live dashboard implies status-during-work (not a frozen terminal)
gaps:
  - No themed/branded loading personality; progress not tokenized — effort: S
soft_goal_delta: functional progress; not thematic

### L100 — Empty & Zero-Data States
score: 1  glyph: △
evidence:
  - CLI/TUI has status surfaces (statusbar.rs) but no confirmed designed empty-state ("no dispatches yet" with CTA)
gaps:
  - No explicit empty-state copy/CTA in TUI dashboard — effort: S
soft_goal_delta: not started

### L101 — Error & Failure State Design
score: 2  glyph: ~
evidence:
  - substrate emits structured JSON dispatch results (errors as structured fields, not raw traces)
  - crates/substrate-trace/src/lib.rs — structured tracing for failures
gaps:
  - TUI error presentation (color-coded, actionable hint) not confirmed styled — effort: S
soft_goal_delta: structured errors; presentation polish unverified

### L102 — Motion & Animation System
score: 1  glyph: △
evidence:
  - TUI redraws (ratatui) provide live updates; grep reduced-motion/prefers → 0 (no reduced-motion handling)
gaps:
  - No motion tokens; no reduced-motion respect — effort: S
soft_goal_delta: not started

### L103 — Splash / Launch Experience
score: 0  glyph: ✗
evidence:
  - No banner/figlet/ascii-art on startup (grep figlet/banner → none); help.rs exists but no branded launch frame
gaps:
  - No startup banner/branded first frame for CLI/TUI — effort: S
soft_goal_delta: not started

### L104 — Theming (Dark / Light / Brand)
score: 1  glyph: △
evidence:
  - 30 `Color::` refs but no theme switch / palette module; no `--theme` flag or terminal-bg adaptation found
gaps:
  - No theme system (single implicit palette) — effort: M
soft_goal_delta: not started

### L105 — Visual Consistency & Brand Cohesion
score: 2  glyph: ~
evidence:
  - crates/substrate-tui/src/components/{dashboard,mod}.rs — componentized TUI widgets (consistent structure)
  - statusbar.rs + help.rs + dashboard.rs share the ratatui idiom
gaps:
  - Consistency is by-convention, not enforced (no shared style module) — effort: S
soft_goal_delta: componentized; no enforced token cohesion

### L106 — Signature / Unique Identity Element
score: 0  glyph: ✗
evidence:
  - No documented signature identity (no IDENTITY.md, no mascot/motif/signature animation); reads as a default ratatui dashboard
gaps:
  - No unique visual signature — the anti-generic pillar unmet — effort: M
soft_goal_delta: not started

### L107 — Visual Spec & Golden Tests
score: 0  glyph: ✗
evidence:
  - MISSING: VISUAL_SPEC.md / docs/visual/ (none) AND no tests/golden/ (grep → none)
gaps:
  - No visual spec + no golden-output regression tests for TUI/CLI — effort: M
soft_goal_delta: not started

CLUSTER_TOTAL score=11/36 pct=31% grade=F
CLUSTER_DONE cluster=C10 repo=substrate
