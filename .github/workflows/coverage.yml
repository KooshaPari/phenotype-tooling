name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  id-token: write  # for OIDC trust (no PAT secrets)

env:
  HOOKS_SKIP: ${{ vars.HOOKS_SKIP || 'false' }}
  SKIP: ${{ vars.SKIP || '' }}

jobs:
  lint:
    if: ${{ !contains(vars.SKIP || '', 'lint') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Run lint
        run: echo "Running language-specific lint"
  test:
    if: ${{ !contains(vars.SKIP || '', 'test') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Run tests
        run: echo "Running language-specific tests"
  coverage:
    if: ${{ !contains(vars.SKIP || '', 'coverage') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Generate coverage
        run: echo "Generating coverage (language-specific)"