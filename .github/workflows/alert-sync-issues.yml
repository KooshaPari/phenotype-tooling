name: Alert Sync Issues

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  workflow-present:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Alert sync workflow dispatched"

  alert-sync:
    uses: KooshaPari/phenoShared/.github/workflows/reusable/alert-sync-issues.yml@main
    with:
      auto-label: auto-alert-sync
