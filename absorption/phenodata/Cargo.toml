# PhenoData - Data Layer Workspace
#
# Contains data-related crates:
# - surreal-bridge: SurrealDB embedded integration
# - pg-bridge: PostgreSQL/pgvector compatibility
# - query: Unified query builder
[workspace]
members = [
    "crates/core",
    "crates/surreal-bridge",
    "crates/pg-bridge",
    "crates/pheno-query",
    "crates/smoke-tests",
]
resolver = "3"

[workspace.package]
license = "MIT OR Apache-2.0"

[workspace.dependencies]
tokio = { version = "1.44", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
anyhow = "1.0"
tracing = "0.1"
tracing-attributes = "0.1"

[workspace.metadata.cargo-machete]
ignored = ["rustls", "tokio-postgres-rustls", "tracing-attributes", "proptest", "tokio"]
