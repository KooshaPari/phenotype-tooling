/**
 * {{id}} — generated by HexaKit mcp-server-ts7 template (PLANNED STUB).
 *
 * Runnable with: bun run src/index.ts
 *
 * NOTE: Imports target MCP TS SDK v2 (@modelcontextprotocol/server).
 * Do not promote to active until sd-ts7 defer gate clears.
 */
import { McpServer } from "@modelcontextprotocol/server";
import { StdioServerTransport } from "@modelcontextprotocol/server";

const server = new McpServer({ name: "{{id}}", version: "0.1.0" });

// TODO(sd-ts7): wire zod schemas + tool registration when v2 API stabilizes
server.tool("{{id}}_ping", async () => ({
  content: [{ type: "text", text: "pong from {{id}}" }],
}));

const transport = new StdioServerTransport();
await server.connect(transport);
