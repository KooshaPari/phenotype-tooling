---
id: {{id}}-skill
title: {{title}} skill
version: 0.1.0
requires_servers:
  - {{id}}
triggers:
  - {{id}}
  - {{id}} ping
permissions:
  - network
---

# {{title}} skill

Use `{{id}}_ping` from the `{{id}}` MCP server.
