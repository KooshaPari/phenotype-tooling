"""{{id}} server.py — generated by HexaKit mcp-server template.

Runnable with: python {{module}}.py
"""
from __future__ import annotations

import os
from fastmcp import FastMCP

mcp = FastMCP("{{id}}")


@mcp.tool()
def {{id}}_ping(message: str) -> str:
    """Echo `message` back to the caller."""
    return message


if __name__ == "__main__":
    mcp.run(transport="{{transport}}")
