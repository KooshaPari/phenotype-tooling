# pheno-letta

Forgecode plugin that spawns the `letta` server as a per-machine systemd unit. Primary adapter for the `Identity` `MemoryScope`.

See `SKILL.md` for the capability manifest, `plugin.toml` for the descriptor, and `systemd/pheno-letta.service` for the unit.
