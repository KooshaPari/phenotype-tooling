# pheno-cognee

Forgecode plugin that spawns the `cognee-mcp` server (project knowledge graph) as a per-machine systemd unit. Primary adapter for the `ProjectKnowledge` `MemoryScope`.

See `SKILL.md` for the capability manifest, `plugin.toml` for the descriptor, and `systemd/pheno-cognee.service` for the unit.
